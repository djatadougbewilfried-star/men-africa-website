import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Import / Export",
  description: "Services d'import-export en Côte d'Ivoire et Afrique de l'Ouest. Sourcing international et logistique.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
